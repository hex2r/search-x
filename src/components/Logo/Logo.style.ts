import styled from "@emotion/styled"

export const Logo = styled.a`
  display: inline-flex;
  align-items: baseline;
  text-decoration: none;
  color: #000;

  svg {
    width: 3rem;
    margin-right: 0.5rem;
  }
`
